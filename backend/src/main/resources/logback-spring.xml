<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ==================== 全局参数 ==================== -->
    <property name="LOG_HOME" value="logs"/>
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId}] %-5level %logger{50} - %msg%n"/>

    <!-- ==================== 控制台输出 ==================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== API接口日志 -> resources/log.txt ==================== -->
    <appender name="API_LOG" class="ch.qos.logback.core.FileAppender">
        <file>src/main/resources/log.txt</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== 文件输出（所有日志） ==================== -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/social-retail.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/social-retail.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== 日志级别控制 ==================== -->

    <!-- 接口层：DEBUG 级别，输出到 log.txt -->
    <logger name="com.socialretail.backend.controller" level="DEBUG" additivity="false">
        <appender-ref ref="API_LOG"/>
    </logger>
    <logger name="com.socialretail.backend.service.merchant" level="DEBUG" additivity="false">
        <appender-ref ref="API_LOG"/>
    </logger>
    <logger name="com.socialretail.backend.service.admin" level="DEBUG" additivity="false">
        <appender-ref ref="API_LOG"/>
    </logger>

    <!-- MyBatis SQL 打印 -->
    <logger name="com.socialretail.backend.mapper" level="DEBUG"/>

    <!-- Spring 框架 -->
    <logger name="org.springframework" level="WARN"/>

    <!-- MyBatis 框架日志 -->
    <logger name="org.mybatis" level="WARN"/>

    <!-- ==================== 根配置 ==================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_ALL"/>
    </root>

</configuration>
