package com.socialretail.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Security is handled by AuthInterceptor, not Spring Security
}
